// Stub — no backend needed for local mode
export async function initSiteChat(_siteId: string, _pageId: string): Promise<void> {}
export async function getOrCreateSiteChat(_siteId: string): Promise<any> { return null }
